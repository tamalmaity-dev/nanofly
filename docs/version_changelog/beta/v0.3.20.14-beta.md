# NanoFly v0.3.20.14-beta — Borderless File Manager Overhaul, Rename Actions & Interactive Image Editor

**Release Date:** 2026-05-30  
**Type:** Feature, Theme Support & Visual Redesign Update  
**Urgency:** High  

---

## Summary

This release introduces a major visual overhaul, usability optimization, and feature expansion for the NanoFly File Manager. The interface now features borderless panels separated by subtle divider rules in a clean developer IDE column layout. Hardcoded dark colors are fully replaced with standard CSS theme variables to fix light-mode compatibility. Additionally, we added Drag-and-Drop uploads, folder-first sorting, folder/file renaming, support for a wider range of media formats, native media asset loading indicators, and an interactive Image Editor that supports scroll wheel zooming, slider zooming, click-drag panning, and realtime style adjustments.

## Key Changes

### 1. Theme Compatibility (Light/Dark Mode Fix)
- **Responsive Colors**: Replaced all hardcoded background and text values in the file manager with theme CSS variables (`var(--bg-base)`, `var(--bg-surface)`, `var(--bg-elevated)`, `var(--border)`, `var(--text-primary)`, etc.).
- **Code Editor Syntax**: Integrated light-theme syntax overrides for Prism highlight tokens, enabling high-contrast code readability across theme modes.
- **Code Editor Background**: Overrode the hardcoded dark background of syntax highlighters with `var(--bg-surface)` (which is a premium dark blue-grey in dark mode, not pure black) to make it integrate beautifully with panels.

### 2. Seamless Borderless Columns Divider Layout
- **IDE Column Layout**: Stripped individual container borders, box shadows, and margins to build a unified column system divided only by vertical separator rules (`1px solid var(--border)`).
- **High-Density Sidebar**: Restyled drives and storage mounts to align with file explorer lists.

### 3. Horizontal Column Sliding & Text Size Improvements
- **Column Constraints**: Applied minimum width restrictions (Drives Sidebar: `180px`, Directory Explorer: `300px`, Workbench Editor/Media: `450px`) combined with `flex-shrink: 0` constraints.
- **Horizontal Scroll**: Set `overflow-x: auto` on the columns wrapper, allowing columns to slide smoothly on smaller windows without collapsing layout content.
- **Enlarged Text**: Increased font sizes of all key items (labels, file lists, details, warnings, headers, code text) to 14px/0.88rem/0.95rem for clear, comfortable reading.
- **Consistent Alignment**: Standardized action buttons inside the file manager header to align perfectly.

### 4. System Disk Partition Filtering
- **System Partitions Skipped**: Updated the Unix drives collector (`drives_unix.go`) to dynamically detect the root system disk partition (e.g. `sda1` or `mmcblk0p2`) and filter out all other partitions belonging to the same physical system drive (like boot, recovery, etc.) from being mounted or reported as "external" storage. Only distinct physical disks (like attached USB drives) are auto-mounted.

### 5. Drag-and-Drop File Uploads
- **Drop Zone Overlay**: Implemented HTML5 drag-and-drop support. Dragging files over the explorer list displays a drop overlay zone to upload multiple items in the background.

### 6. Folder-First Sorting Controls
- **Metrics Dropdown**: Sort folder lists dynamically by **Name**, **Size**, or **Last Modified Date** (Ascending/Descending).
- **Priority Directories**: Directory items are automatically prioritized and kept at the top of the sorted lists.

### 7. Files/Folders Renaming (API & UI Modal)
- **Rename Endpoint**: Registered `/api/v1/files/rename` on the Go backend using standard `os.Rename`.
- **Rename Dialog**: Added inline Rename row buttons that trigger a clean prompt input modal to submit rename requests.

### 8. Interactive Image Editor
- **Multi-modal Zoom**: Scroll over the canvas to zoom in/out smoothly, or use the integrated Zoom slider bar and +/- buttons with precise percentage indicators.
- **Drag Panning**: Click and drag zoomed images in any direction.
- **Grid Filter Adjustments**: Arranged adjustment sliders (Brightness, Contrast, Grayscale, Sepia), Invert checkbox, Rotate 90°, and Reset controls in a uniform, professional grid panel.

### 9. Expanded Media Formats & Native Loaders
- **Wide Extensions Support**: Enabled native playback recognition for a broader range of media:
  - **Images**: `.heic`, `.avif`, `.webp`, `.tiff`
  - **Audio**: `.m4a`, `.aac`, `.wma`
  - **Video**: `.mov`, `.mkv`, `.flv`, `.wmv`
- **Native Loading Indicators**: Hooked state-driven `mediaLoading` spinner overlays to native player events (`onLoad` for images, `onCanPlay` for audio/video) to display loading feedback during asset fetching.

---

## Verification Results

- **Go Backend**: Verified compilation passes successfully with `go build ./...`.
- **React Frontend**: Verified compilation and production asset bundling pass successfully with `npm run build`.




--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------



Viewed VERSION:1-2

I will search the Go backend files for the drives detection or mount point extraction logic.

Searched for "drives"

I will view the Unix drives detection code in `drives_unix.go` to inspect the logic.

Viewed drives_unix.go:1-201

I will update the Unix drives detection code in `drives_unix.go` to exclude any partitions belonging to the active system root disk (OS partition, boot sector, recovery partition, etc.) from being mounted or reported as "external" storage devices. This matches the standard behavior of local operating systems.

Edited drives_unix.go

I will run the Go backend compilation to ensure `drives_unix.go` is syntactically correct and builds cleanly.

Ran command: `go build ./...`

I will search for color variables inside the main stylesheet `index.css` to check the palette for dark and light modes.

Searched for "bg-base"

I will search for color variables prefixed with `--bg` in `index.css`.

Searched for "--bg"

I will view the first 100 lines of `index.css` to check the theme colors.

Viewed index.css:1-100

I will view the main header and breadcrumbs layout section of `FileManager.jsx` to check button styles and text sizes.

Viewed FileManager.jsx:913-1007

I will view lines 1070 to 1180 of `FileManager.jsx` to verify the page header and button layout rendering.

Viewed FileManager.jsx:1070-1180

I will view lines 1180 to 1250 of `FileManager.jsx` to locate text font sizes inside the Sidebar Column and the Directory Explorer Column.

Viewed FileManager.jsx:1180-1250

I will view lines 1250 to 1350 of `FileManager.jsx` to locate text font sizes inside the explorer panel search, headers, and sorting dropdown.

Viewed FileManager.jsx:1250-1350

I will view lines 1350 to 1460 of `FileManager.jsx` to check file and folder row item text sizes.

Viewed FileManager.jsx:1350-1460

I will view lines 1530 to 1600 of `FileManager.jsx` to locate text font sizes inside the Workbench Editor Header and other preview sections.

Viewed FileManager.jsx:1530-1600

I will edit `CodeEditor.jsx` to increase the font size of the code text to `14px`, override the background so it uses the theme surface variable (`var(--bg-surface)`) instead of pure black, and add transparent background overrides for the syntax highlighting blocks.

Edited CodeEditor.jsx

I will add a transparent background override to the code editor's textareas and pre tags in `CodeEditor.jsx` to let the surface theme show through properly.

Edited CodeEditor.jsx

I will perform the UI revisions on `FileManager.jsx` to increase the font sizes of file/folder names, header titles, subtexts, and breadcrumbs, as well as ensure perfect vertical alignment on top-level action buttons.

Edited FileManager.jsx

I will search the frontend code for the "Drives & Storage" header to see how the dashboard metrics cards are rendered and handle the drive types.

Searched for "Drives & Storage"

I will view the drives cards section in `Dashboard.jsx` to make sure it aligns with our backend model.

Viewed Dashboard.jsx:460-520

I will run the React frontend build to check for any build or syntax errors with our UI improvements.

Ran command: `npm run build`

I will compile the Go backend to check that `GetDrives()` changes are valid.

Ran command: `go build ./...`

I will check the git status to see the list of modified files.

Ran command: `git status`

I will update the `v0.3.20.14-beta.md` changelog to detail the drive detection fixes, code editor theme adjustment, text enlargement, and button alignments.

Edited v0.3.20.14-beta.md

I will view the verification and details section of `walkthrough.md` to update it.

Viewed walkthrough.md:30-60

I will update the `walkthrough.md` file to add documentation for system partition filtering, editor background customization, text size enlargement, and button alignment.

Edited walkthrough.md

I will check `git status` to see the updated files list.

Ran command: `git status`

I will stage the updated files with git add.

Ran command: `git add -f docs/version_changelog/beta/v0.3.20.14-beta.md; git add internal/api/files/drives_unix.go web/src/components/CodeEditor.jsx web/src/pages/FileManager.jsx`

I will commit the changes with a clear and detailed commit message.

Ran command: `git commit -m "fix: skip active system root disk partitions in Unix drives detection, increase File Manager font sizes, align action buttons, and customize CodeEditor background theme color"`

I will update the local release tag to point to our latest build.

Ran command: `git tag -fa v0.3.20.14-beta -m "release: v0.3.20.14-beta"`

I will push the commits to the origin main branch.

Ran command: `git push origin main`

I will push the updated tag to GitHub.

Ran command: `git push -f origin v0.3.20.14-beta`

I will check git status to verify the workspace is clean.

Ran command: `git status`

I have successfully fixed the phantom drive reporting issue, customized the editor backgrounds, adjusted text sizes, aligned the buttons, and pushed the updated release commit and tag to GitHub!

### Improvements Implemented

1. **System Disk Partitions Skip (Drives & Storage Fix)**
   - Updated the Unix drives collector logic in [drives_unix.go](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/internal/api/files/drives_unix.go) to dynamically extract the device name of the system root `/` partition (e.g. `sda1` or `mmcblk0p2`).
   - Skips auto-mounting or listing any other partitions belonging to the same physical disk (such as `sda2` or `mmcblk0p1` boot/recovery/EFI partitions). 
   - This prevents phantom external drives from populating. True external drives (like attached USB storage) will continue to auto-mount and show up as expected.

2. **Code Editor Theme Background Customization**
   - Modified [CodeEditor.jsx](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/web/src/components/CodeEditor.jsx) to override the hardcoded black style of Prism tomorrow.
   - Rebound it to `var(--bg-surface)`, resolving the pure black color blocks. It now matches the dark blue-grey of other panels in dark mode and switches to white in light mode.
   - Increased the editor text font size to `14px` for better code readability.

3. **Typography & Layout Tuning**
   - Increased font sizes across [FileManager.jsx](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/web/src/pages/FileManager.jsx) (folders/files lists, drive items, alerts, headers, breadcrumbs) to `0.88rem` / `0.95rem` to improve readability.
   - Standardized the heights of header buttons to a clean, uniform size (`30px`) and properly aligned them.

4. **Git Release Finalization**
   - Added these changes to the release logs in [v0.3.20.14-beta.md](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/docs/version_changelog/beta/v0.3.20.14-beta.md).
   - Committed the changes, rebuilt both frontend and backend successfully, and pushed the tag update to GitHub:
     ```bash
     To https://github.com/tamalmaity-dev/nanofly.git
        41c56e2..09aeb1c  main -> main
      + 87deb40...dc40ec3 v0.3.20.14-beta -> v0.3.20.14-beta (forced update)
     ```