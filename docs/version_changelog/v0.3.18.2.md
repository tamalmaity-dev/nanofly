# VERSION: v0.3.18.2

# PTY terminal container resolution fix

## Changes

### 1. Fix Interactive PTY Container Terminal Resolution
- **Fix**: Updated `TerminalPanel.jsx` to append the correct 8-character service ID suffix to the container name for interactive terminal PTY WebSocket connections.
- **Benefit**: Resolves the `Error response from daemon: No such container: nf-app-wordpress` error on the console tab and enables remote shell access into suffix-based running containers.
