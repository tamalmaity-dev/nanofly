# VERSION: v0.3.18.1

# Enforce Unlimited Resource Tier for Database Containers

## Changes

### 1. Default Database Containers to Unlimited Resource Tier
- **Fix**: Updated `CreateDB` in [manager.go](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/internal/api/docker/manager.go) to always assign the `"unlimited"` resource tier to database containers, ignoring default service tiers (`micro`, `nano`, `standard`, `large`).
- **Overriding**: Custom limits defined explicitly by the user are still respected.
- **Benefit**: Fully prevents database containers (e.g. MySQL 8.0, PostgreSQL) from being terminated on startup or operation due to kernel Out-Of-Memory (OOM) killer constraints, ensuring high-availability database operation on low-resource environments (like Raspberry Pi 3).




I will search for `GetTierWithCustom` to see where the resource tiers and memory/CPU limits are defined.

Searched for "GetTierWithCustom"

I will view `internal/api/docker/limits.go` to see how the resource tiers and memory limits are defined.

Viewed limits.go:1-79

I will modify `manager.go` to enforce the `unlimited` resource tier for all databases by default (unless custom limits are explicitly defined by the user). This eliminates any memory limits and avoids Out-Of-Memory (OOM) crashes. Note that Docker containers do not impose disk size limits by default, so they already have unlimited access to the host disk.

Edited manager.go

I will run the Go build command to ensure the backend compiles cleanly after this modification.

Ran command: `go build ./...`

I will update the `VERSION` file to `0.3.18.1` for this update.

Edited VERSION

I will view `docs/version_changelog/v0.3.18.md` to update it with the database unlimited resource mapping details.

Viewed v0.3.18.md:1-94

I will create a new changelog file at `docs/version_changelog/v0.3.18.1.md` specifically for the 0.3.18.1 patch release, documenting the enforcement of the unlimited tier on database services.

Edited v0.3.18.1.md

I will stage all updated and new files (forcing staging for the ignored changelog), create the `v0.3.18.1` commit and tag, and push everything to GitHub.

Ran command: `git add . ; git add -f docs/version_changelog/v0.3.18.1.md ; git commit -m "feat: release version 0.3.18.1 enforcing unlimited resource tier on database services" ; git tag v0.3.18.1 ; git push origin main --tags`

I have successfully implemented the change to enforce the `unlimited` resource tier for all database containers by default, and pushed the new version to GitHub.

### Release Details for `0.3.18.1`:
1. **Force Unlimited DB RAM/CPU**:
   - In `CreateDB` ([manager.go](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/internal/api/docker/manager.go)), database containers are now strictly routed to the `"unlimited"` resource tier. This eliminates default memory constraints (such as `micro` or `standard`) that lead to container exit code `137` (OOM kill).
   - If you set explicit custom memory/CPU overrides in settings, those are still respected.
   - Note: Docker does not place limits on disk space by default, so disk utilization is already unlimited.
2. **Version Bump**:
   - Bumped the version to `0.3.18.1` in [VERSION](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/VERSION).
3. **Changelog**:
   - Created the detailed release changelog at [v0.3.18.1.md](file:///e:/All%20Coding%20Projects/Production%20Projects/NanoFly/docs/version_changelog/v0.3.18.1.md).
4. **Git push**:
   - Committed, tagged as `v0.3.18.1`, and successfully pushed to your GitHub repository.

Please pull version `0.3.18.1` on your Raspberry Pi, run the cleanup commands from earlier, and redeploy!
